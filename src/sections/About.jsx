import Reveal from "../components/Reveal";

export default function About() {
  return (
    <section id="about" className="section">
      <Reveal>
        <h2>About</h2>
        <p>
          I’m a Computer Science student with hands-on experience in full-stack development
          and ML-driven projects. I like building data-driven apps, dashboards, and clean UI.
        </p>
      </Reveal>
    </section>
  );
}